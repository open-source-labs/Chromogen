"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const WarningAggregator_1 = require("../WarningAggregator");
function getIcons(config) {
    var _a;
    // Until we add support applying icon config we just test if the user has configured the icon
    // so we can warn
    if (config.icon || ((_a = config.ios) === null || _a === void 0 ? void 0 : _a.icon)) {
        return true;
    }
    else {
        return false;
    }
}
exports.getIcons = getIcons;
function setIconsAsync(config, projectRoot) {
    const icon = getIcons(config);
    if (!icon) {
        return;
    }
    WarningAggregator_1.addWarningIOS('icon', 'This is the image that your app uses on your home screen, you will need to configure it manually.');
}
exports.setIconsAsync = setIconsAsync;
//# sourceMappingURL=Icons.js.map
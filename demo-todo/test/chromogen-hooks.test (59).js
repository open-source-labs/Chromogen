import { renderHook } from '@testing-library/react-hooks';
   import React, { useState } from 'react';
   import { 
   todo

} from '<ADD USESTATE HOOK FILEPATH >';

describe('USESTATE', () => {
  
  it('should show that current state in todo is not null or undefined', () => {
    expect(drink).not.toBe(undefined);
    expect(drink).not.toBe(null);
  });

it('should show that state in add changes after every dispatch', () => {
  expect(1).not.toBe(0)
}
      );
	it('should show that state in subtract changes after every dispatch', () => {
        expect(-1).not.toBe(0)}
      }
	it('should show that state in todo changes after every dispatch', () => {
        expect(sleep).not.toBe()}
      }
	it('should show that state in todo changes after every dispatch', () => {
        expect(eat).not.toBe(sleep)}
      }
	
});